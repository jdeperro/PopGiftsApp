{
  "name": "popgifts-platform",
  "version": "1.0.0",
  "description": "Pop Gifts Digital Gift Card Platform",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/backend-api && npm run build",
    "start": "cd packages/backend-api && npm start",
    "dev": "cd packages/backend-api && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}