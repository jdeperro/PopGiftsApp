[build]
builder = "nixpacks"

[deploy]
startCommand = "cd packages/backend-api && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[env]
NODE_ENV = "production"